import InvestMentCard from './InvestMentCard';
import MonthlyReturnCard from './MonthlyReturnCard';
import ContactAssetDetail from './ContactAssetDetail';
import VisualReturns from './VisualReturns';
import HowItWorks from './HowItWorks';
import CkycDialog from './CkycDialog';

export const UccInProgressData = {
  title: 'Your registration with the NSE is in progress',
  amoTitle: 'Your AMO cannot be placed',
  description:
    'To invest via the RFQ, every investor must be registered with the NSE. This usually happens instantly, but currently there seems to be a delay of upto 1 hr. We will notify you via email as soon as the same is completed.',
  subtitle:
    ' Apologies for the inconvenience but we will have your account up and running shortly.',
};

export {
  MonthlyReturnCard,
  InvestMentCard,
  ContactAssetDetail,
  VisualReturns,
  HowItWorks,
  CkycDialog,
};
